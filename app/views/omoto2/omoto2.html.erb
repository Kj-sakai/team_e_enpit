<div class="starter-template">
  <h1>Rails商品</h1>
    <table class="table table-hover">
      <tr>
        <th>商品名</th>
        <th>価格</th>
      </tr>
      <% @items.each do |item| %>
      <tr>    
        <td><%= item.itemName %></td>
        <td><%= item.itemPrice %></td>
      </tr>
      <% end %>
</div>
